import 'package:flutter/material.dart';

class UnreadPage extends StatelessWidget {
  const UnreadPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('unread page'),
      ),
    );
  }
}
